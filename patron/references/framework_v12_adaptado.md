# Framework v12 Adapted (Legacy Reference)

This file is retained only as historical context.

## Important
- It is superseded by the v14 Split-Knowledge architecture.
- Do not use this file as an operational source for the current Custom GPT.
- The current operational files are:
  - `server/gpt_instructions_v14_partA.txt`
  - `server/openapi.yaml`
  - `ALGORITMOS VERSÃO ORIGINAL/mcts_engine_v2.py` (attached to the GPT, not exposed by Actions)

## Legacy status
- Prior versions depended on action-delivered algorithm fragments.
- That approach is no longer approved for secure deployment because tool activity can expose payload contents.
- Keep this document only for migration history.
